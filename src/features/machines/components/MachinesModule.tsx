"use client";

import Link from "next/link";
import { ModuleHeader, secondaryButton, StatusPill } from "@/components/ui/ModuleUi";
import { useDemoData } from "@/features/demo/services/demo-repository";

export function MachinesModule() { const data = useDemoData(); return <div className="mx-auto max-w-7xl"><ModuleHeader eyebrow="Capacités de production" title="Parc Machines" description="Vue simple des capacités, indisponibilités, maintenances et actions ouvertes." actions={<Link href="/machines/maintenance" className={secondaryButton}>Planning maintenance</Link>} /><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{data.machines.map((machine) => { const maintenance = data.maintenance.filter((item) => item.machineId === machine.id && item.status !== "Terminée").sort((a, b) => a.date.localeCompare(b.date))[0]; const actions = data.actions.filter((item) => item.machineId === machine.id && item.status !== "Terminée"); return <Link href={`/machines/${machine.id}`} key={machine.id} className="overflow-hidden rounded-2xl border border-[var(--app-border)] bg-white shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><div className="grid h-36 place-items-center bg-gradient-to-br from-slate-100 to-slate-200 text-4xl font-bold text-slate-400">{machine.id}</div><div className="p-5"><div className="flex items-start justify-between gap-2"><div><h2 className="font-semibold">{machine.displayName}</h2><p className="mt-1 text-xs text-slate-500">{machine.department} · {machine.type}</p></div><StatusPill tone={machine.status === "En panne" ? "danger" : machine.status === "Disponible" ? "success" : machine.status === "Maintenance prévue" ? "warning" : "info"}>{machine.status}</StatusPill></div><dl className="mt-4 grid grid-cols-2 gap-3 text-sm"><div><dt className="text-xs text-slate-500">Prochaine maintenance</dt><dd className="mt-1 font-medium">{maintenance?.date.slice(0, 10) ?? "Aucune"}</dd></div><div><dt className="text-xs text-slate-500">Actions ouvertes</dt><dd className="mt-1 font-medium">{actions.length}</dd></div></dl></div></Link>; })}</div></div>; }
