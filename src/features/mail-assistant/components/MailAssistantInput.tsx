import { MailAssistantVoiceInput } from "@/features/mail-assistant/components/MailAssistantVoiceInput";

export function MailAssistantInput({ value, isBusy, error, onChange, onSend }: { value: string; isBusy: boolean; error: string | null; onChange: (value: string) => void; onSend: () => void }) {
  return <div className="sticky bottom-0 z-20 mt-10 border-t border-black/[0.05] bg-[#f7f8f6]/92 py-4 backdrop-blur-xl"><div className="rounded-2xl border border-black/[0.08] bg-white p-2 shadow-[0_12px_40px_rgba(27,41,35,0.08)]"><label htmlFor="mail-assistant-command" className="sr-only">Instruction pour l’assistant</label><textarea id="mail-assistant-command" value={value} onChange={(event) => onChange(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); onSend(); } }} rows={2} placeholder="Demandez une modification ou donnez votre accord…" className="w-full resize-none border-0 bg-transparent px-3 py-2 text-sm outline-none"/><div className="flex items-end justify-between gap-3"><MailAssistantVoiceInput disabled={isBusy} onTranscript={onChange}/><button type="button" disabled={isBusy || !value.trim()} onClick={onSend} className="min-h-11 rounded-xl bg-[#1f5f49] px-5 text-sm font-semibold text-white disabled:opacity-40">{isBusy ? "Traitement…" : "Envoyer"}</button></div></div>{error ? <p role="alert" className="mt-2 text-sm text-red-700">{error}</p> : null}</div>;
}
