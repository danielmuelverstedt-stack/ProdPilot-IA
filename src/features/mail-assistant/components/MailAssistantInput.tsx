import { MailAssistantVoiceInput } from "@/features/mail-assistant/components/MailAssistantVoiceInput";
import type { MailAssistantStartSettings } from "@/features/mail-assistant/types/mail-assistant";
export function MailAssistantInput({ value, isBusy, error, settings, autoStartToken = 0, onChange, onSend, onStop, onVoiceSubmit }: { value: string; isBusy: boolean; error: string | null; settings: MailAssistantStartSettings; autoStartToken?: number; onChange: (value: string) => void; onSend: () => void; onStop: () => void; onVoiceSubmit: (value: string) => void }) {
  return <div className="sticky bottom-0 z-20 mt-10 border-t border-[var(--app-border)] bg-[var(--app-background)]/92 py-4 backdrop-blur-xl"><div className="rounded-2xl border border-[var(--app-border)] bg-white p-2 shadow-[var(--app-shadow-md)]"><label htmlFor="mail-assistant-command" className="sr-only">Instruction pour l’assistant</label><textarea id="mail-assistant-command" value={value} onChange={(event) => onChange(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); onSend(); } }} rows={2} placeholder="Parlez ou saisissez une instruction…" className="w-full resize-none border-0 bg-transparent px-3 py-2 text-sm outline-none"/><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><MailAssistantVoiceInput disabled={isBusy} settings={settings} autoStartToken={autoStartToken} onTranscript={onChange} onSubmit={onVoiceSubmit}/>{isBusy ? <button type="button" onClick={onStop} className="min-h-11 rounded-xl border border-red-300 bg-red-50 px-5 text-sm font-semibold text-red-800">Interrompre</button> : <button type="button" disabled={!value.trim()} onClick={onSend} className="min-h-11 rounded-xl bg-[var(--app-primary)] px-5 text-sm font-semibold text-white hover:opacity-90 disabled:opacity-40">Envoyer</button>}</div></div>{error ? <p role="alert" className="mt-2 text-sm text-red-700">{error}</p> : null}</div>;
}
