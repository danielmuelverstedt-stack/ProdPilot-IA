import type { MailAssistantSessionMessage } from "@/features/mail-assistant/types/mail-assistant";

export function MailOriginalMessageDrawer({ message, source, onClose }: { message: MailAssistantSessionMessage | null; source: { href: string | null; label: string } | null; onClose: () => void }) {
  if (!message) return null;
  return <div className="fixed inset-0 z-50 bg-slate-950/25 backdrop-blur-sm" role="presentation" onMouseDown={(event) => { if (event.currentTarget === event.target) onClose(); }} onKeyDown={(event) => { if (event.key === "Escape") onClose(); }}><aside role="dialog" aria-modal="true" aria-labelledby="original-message-title" tabIndex={-1} className="absolute inset-x-0 bottom-0 max-h-[90vh] overflow-y-auto rounded-t-[2rem] bg-white p-6 shadow-2xl sm:inset-y-0 sm:left-auto sm:w-[32rem] sm:rounded-none sm:p-8"><div className="flex items-start justify-between gap-4"><div><p className="text-sm text-slate-500">{message.from.name ?? message.from.email}</p><h2 id="original-message-title" className="mt-1 text-xl font-semibold">{message.subject}</h2></div><button type="button" onClick={onClose} className="min-h-10 rounded-xl px-3 text-sm font-semibold">Fermer</button></div><p className="mt-6 whitespace-pre-wrap text-sm leading-7 text-slate-700">{message.bodyText}</p>{source?.href ? <a href={source.href} target="_blank" rel="noreferrer" className="mt-6 inline-flex min-h-11 items-center rounded-xl border border-black/10 px-4 text-sm font-semibold text-[var(--app-primary)]">{source.label}</a> : source ? <p className="mt-6 text-sm text-slate-500">{source.label}</p> : null}</aside></div>;
}
